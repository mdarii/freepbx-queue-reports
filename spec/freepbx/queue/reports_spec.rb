require 'spec_helper'

describe Freepbx::Queue::Reports do
  it 'has a version number' do
    expect(Freepbx::Queue::Reports::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
